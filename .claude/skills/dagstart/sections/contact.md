# Contact-context slot — nog niet gekoppeld

Unlocked by: **blok J** (specifically "Briefings voorbereiden" en "Vergaderoutput verwerken" — see takencatalogus §7 items 10-11, suggested as the first two block-J stories in `pa-agent-plan.md` §8 "Doorlooptijd"). Also depends on `personal/stakeholders.md` existing with real content — that file is referenced once already (in `agenda-briefing`'s "Learning from corrections" routing note) but has never been created; it starts empty and fills organically, same rule as every `personal/` file (§6).

## Intended scope (from the source brainstorm, for when block J lands)

- Per agenda item: who's attending.
- Last contact with that person (mail thread or previous meeting notes) — depends on both the inbox capability (C1) and a decisions/actions record (`decisions-actions.md`, mentioned under catalog item 11).
- Relevant documents staged for the meeting — this is catalog item 21 ("Documenten voorbereiden"), a further-out block-J story, not assumed available here.
- Open action items from the previous meeting with the same people.

This slot has the most upstream dependencies of the six (mail history + meeting-output records + stakeholder file). Expect it to land later than inbox/taken even though block J is nominally "next" after I.

## Not in this draft

Everything — this is a placeholder.
