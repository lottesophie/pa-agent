# Contact-context slot — nog niet gekoppeld

Unlocked by: **"briefings voorbereiden"** and **"vergaderoutput verwerken"** — both sit in the takencatalogus in the backlog and have no story number yet. Also depends on `personal/stakeholders.md` existing with real content — that file is referenced once already (in `agenda-briefing`'s "Learning from corrections" routing note) but has never been created; it starts empty and fills organically, same rule as every `personal/` file.

## Intended scope (from the source brainstorm, for when those stories land)

- Per agenda item: who's attending.
- Last contact with that person (mail thread or previous meeting notes) — depends on both the inbox capability (pa-6) and a decisions/actions record (`decisions-actions.md`, which belongs to "vergaderoutput verwerken").
- Relevant documents staged for the meeting — this is "documenten voorbereiden" in the takencatalogus, further out still, not assumed available here.
- Open action items from the previous meeting with the same people.

This slot has the most upstream dependencies of the six (mail history + meeting-output records + stakeholder file). Expect it to land later than inbox/taken, regardless of where its source stories end up in the order.

## Not in this draft

Everything — this is a placeholder.
