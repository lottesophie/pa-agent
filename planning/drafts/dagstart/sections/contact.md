# Contact-context slot — nog niet gekoppeld

Unlocked by: **blok H** (specifically "Briefings voorbereiden" and "Vergaderoutput verwerken" — see takencatalogus §7 items 10–11, already marked as the suggested first two block-H stories in `pa-agent-plan.md` §8). Also depends on `personal/stakeholders.md` existing with real content — that file is referenced once already (in `agenda-briefing`'s "Learning from corrections" routing note) but has never been created; it starts empty and fills organically, same rule as every `personal/` file (§6).

## Intended scope (from the source brainstorm, for when block H lands)

- Per agenda item: who's attending.
- Last contact with that person (mail thread or previous meeting notes) — depends on both the inbox capability (C1) and a decisions/actions record (`decisions-actions.md`, mentioned under catalog item 11).
- Relevant documents staged for the meeting — this is catalog item 21 ("Documenten voorbereiden"), a further-out block-H story, not assumed available here.
- Open action items from the previous meeting with the same people.

This slot has the most upstream dependencies of the six (mail history + meeting-output records + stakeholder file). Expect it to land later than inbox/taken even though block H is nominally "next" after F.

## Not in this draft

Everything — this is a placeholder.
